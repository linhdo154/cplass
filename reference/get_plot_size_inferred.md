# Compute shared plot bounds across a list of inferred paths

Compute shared plot bounds across a list of inferred paths

## Usage

``` r
get_plot_size_inferred(path_list)
```

## Arguments

- path_list:

  a list of path objects, each with \`\$path_inferred\`.
