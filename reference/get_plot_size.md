# Compute shared plot bounds across a list of ground-truth paths

Compute shared plot bounds across a list of ground-truth paths

## Usage

``` r
get_plot_size(path_list)
```

## Arguments

- path_list:

  a list of path objects, each with \`\$path\`.
